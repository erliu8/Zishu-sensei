# 前端环境变量配置
# 浏览器端使用的API地址（通过 Next.js rewrites 代理到后端）
NEXT_PUBLIC_API_URL=http://localhost:8001/api/v1

# 服务器端使用的后端地址（用于 SSR 和 API rewrites）
BACKEND_API_URL=http://localhost:8001/api/v1

# WebSocket 地址
NEXT_PUBLIC_WS_URL=ws://localhost:8001/ws
