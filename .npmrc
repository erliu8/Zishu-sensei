# Workspace configuration
# Ensure dependencies are hoisted to allow proper resolution
node-linker=hoisted
public-hoist-pattern[]=*

# Optional: Use faster npm registry (comment out for CI)
# registry=https://registry.npmmirror.com/

